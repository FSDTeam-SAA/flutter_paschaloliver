
class AppDependencies {
  static void init() {
    //final apiClient = ApiClient(baseUrl);

 /*    Get.put(AuthRepository(ApiClient(baseUrl)));

    Get.put(RoleController(), permanent: true);
    Get.put(UserProfileRepository(ApiClient(baseUrl)));
    Get.put(ProvidersServiceRepo(ApiClient(baseUrl)));
    Get.put(ProviderBookingRepo(ApiClient(baseUrl)));
    Get.put(BookingHistoyRepo(ApiClient(baseUrl)));
    Get.put(ReviewRepo(ApiClient(baseUrl)));

    Get.put(AuthController(Get.find<AuthRepository>()));
    Get.put(UserProfileController(Get.find<UserProfileRepository>()));
    Get.put(ProvidersServicesController(Get.find<ProvidersServiceRepo>()));
    Get.put(BookAppointmentController(Get.find<ProviderBookingRepo>()));
    Get.put(BookingHistoryController(Get.find<BookingHistoyRepo>()));
    Get.put(ReviewController(Get.find<ReviewRepo>())); */
  }
}
